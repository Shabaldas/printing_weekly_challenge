<div id="juries">
  <!-- Breadcrumb -->
  <div class="container">
    <a class="flex items-center gap-6 text-xl leading-[26px] my-6 w-fit" href="/">
      <svg class="rotate-180" width="11" height="18" viewBox="0 0 11 18" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M1.31383 1.68617L8.7844 9.15674L1.31383 16.6273" stroke="#2A343E" stroke-width="2"/>
      </svg>

      Back To Home Page
    </a>
  </div>

  <!-- Hero section -->
  <div class="container">
    <div class="pb-[131px] flex gap-[118px] relative">
      <%= image_tag("cube3.svg", size: "810x457", alt: "img", class: "absolute bottom-0 right-0 z-[-1]") %>

      <h2 class="title leading-[1.2] w-full max-w-[554px]">Meet Our Jury</h2>

      <div class="flex flex-col items-start gap-9 mt-3">
        <p class="text-xl">
          In order to create unforgettible experience for our paticipants we invided the leaders of the masket.
          <br /><br />
          Here you can meet some outstanding figures of 3d printing industry.
        </p>

        <a class="button" href="challenges">All Challenges</a>
      </div>
    </div>
  </div>

  <!-- Juries -->
  <div class="bg-creamlight pb-24 pt-4">
    <div class="container">
      <% if @juries.any? %>
        <div class="grid grid-cols-2 gap-x-8">
          <% @juries.each do |jury| %>
            <%= link_to(jury_path(jury.id)) do %>
              <div class="flex items-center gap-8 py-6 border-b-[1px] border-darkblue">
                <%= image_tag(jury.avatar, size: "192x208", alt: "img", class: "w-full max-w-[192px] object-cover") %>
                <div class="flex flex-col items-start gap-8">
                  <div>
                    <h6 class="font-bold line-clamp-1"><%= jury.nick_name %></h6>
                    <p class="line-clamp-1"><%= "#{jury.first_name} #{jury.last_name}" %></p>
                  </div>

                  <p class="line-clamp-3 max-w-[256px]">A little description about the work I made. Fantastic story, that I tell.</p>
                </div>
              </div>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <div class="center">
          <div class="flex items-center gap-8 py-6 border-b-[1px] border-darkblue">
            <h2 class="title text-center">There are No available Juries at the moment</h2>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
